// Worker process — processes background jobs from queues
// BullMQ workers will be registered here in Phase 2

console.log("👷 Worker process starting...");
console.log("   No queues configured yet. Add BullMQ workers in Phase 2.");

// Keep process alive
setInterval(() => {}, 60_000);
