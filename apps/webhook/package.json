{
  "name": "@whatsapp-crm/webhook",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@upstash/redis": "^1.35.4",
    "fastify-raw-body": "^5.0.0",
    "@whatsapp-crm/database": "workspace:*",
    "@whatsapp-crm/shared": "workspace:*",
    "fastify": "^5.2.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@whatsapp-crm/eslint-config": "workspace:*",
    "@whatsapp-crm/typescript-config": "workspace:*",
    "tsx": "^4.19.0",
    "typescript": "^5.8.3"
  }
}
